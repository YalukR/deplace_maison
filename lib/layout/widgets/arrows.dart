import 'package:flutter/material.dart';

class ArrowsWidget extends StatelessWidget {
  const ArrowsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Arrows works'));
  }
}
